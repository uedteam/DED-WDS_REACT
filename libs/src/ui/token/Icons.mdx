import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import * as Icons from '../../assets';

<Meta title="Token/Icons" />

<IconGallery>
  {Object.entries(Icons)
    .filter(([name]) => name !== 'iconList')
    .map(([name, Icon]) => (
      <IconItem key={name} name={name}>
        <Icon />
      </IconItem>
    ))}
</IconGallery>
